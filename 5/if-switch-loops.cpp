#include <iostream>
#include <cmath>

using namespace std;

// Y=sqrt(a-b);
int main() {
    unsigned short a;
    cout << "Ievadiet a (datu tips unsigned short) \n";
    if( cin >> a ) {
        cout << "D";
    }
    return 1;
}